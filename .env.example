# Copy this file to .env at the repo root before running docker-compose.
# Note: MinIO is run natively on this machine (not via docker-compose) — see README.md.

# --- PostgreSQL (used by docker-compose) ---
POSTGRES_DB=edtech
POSTGRES_USER=edtech
POSTGRES_PASSWORD=edtech_dev_password
